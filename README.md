## Thread Pool in C++

A minimal thread pool class that allows users to add jobs to a queue which will be picked up by a specified amount of worker threads.

Todo:
- [ ] Implement templated functions & arguments for workers
